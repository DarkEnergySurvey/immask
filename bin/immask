#!/usr/bin/env python
import sys
import time
import immask

# Get the start time
t0 = time.time()

parser = immask.cmdline()
args = parser.parse_args()
args.func(args)

print >>sys.stderr,"# Time:%s" % immask.elapsed_time(t0)

